---
name: remove as.scm
description: Delete the leftover Racket entry point as.scm, completing the cleanup started in session 006.
type: project
---

# Handoff: remove `as.scm` — 2026-04-25

## What was accomplished

Deleted `as.scm` from the repo root.

`as.scm` was the original Racket entry point (`racket -f as.scm`) that `require`d `ac.scm` and `brackets.scm`. Both of those files were deleted in session 006 (`2026-04-25-006-shebang-and-cleanup.md`), at which point `as.scm` became non-functional but was left in place because the user had only asked for the other two to be removed. That session's handoff explicitly flagged `as.scm` for deletion in a future session — this is that deletion.

## Key decisions

- **No replacement / redirect file.** The SBCL entry point is invoked differently (see `how-to-run-news.md`); there is no need for a stub `as.scm` pointing at the new flow.
- **No other code changes.** Grep confirmed the only remaining references to `as.scm` were in handoff docs (which are historical records and were left untouched) and the file's own self-referential comment.

## Files changed this session

- `as.scm` — deleted.

## Current state

The repo no longer contains any Racket-era source files. The only mentions of `as.scm` in the tree are in `docs/agents/handoff/` as historical context.
